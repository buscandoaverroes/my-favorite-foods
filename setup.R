# setup.R
# DESCRIPTION: performs setup actions only to be run once
# see https://rstudio.github.io/renv/reference/init.html for settings

if (FALSE) {
 
  # key settings
  # renv::settings$r.version("4.1.2")
  
  renv::init()

}